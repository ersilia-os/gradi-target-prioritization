pandas>=2.2
pyarrow>=15
requests>=2.31
biopython>=1.83
tqdm>=4.66
tenacity>=8.2

# Protein language model embeddings (scripts/01_esmc_embeddings.py)
torch>=2.2
esm>=3.1   # EvolutionaryScale ESM-C local inference
httpx>=0.27  # required by esm.sdk (not auto-installed by esm)

# Embedding projection / visualization (scripts/02_esm_projection.py)
openTSNE>=1.0   # multiscale + cosine t-SNE (the projection)
umap-learn>=0.5  # used to derive coherent cluster colors
matplotlib>=3.8
colorcet>=3.0   # glasbey categorical palette for many-family coloring
matplotlib-venn>=1.1  # for downstream plotting of the 3-way orthology tables (03c outputs)

# AlphaFold structural annotation (scripts/06_alphafold_structure.py)
networkx>=3.0   # PAE-graph community detection for domain segmentation

# Essentiality assessment (scripts/07*)
openpyxl>=3.1   # read the ECL8 / Goodall supplementary .xlsx tables (07a/07b/07c)
cobra>=0.29     # genome-scale metabolic FBA single-gene deletion on iYL1228 (07f)
scikit-learn>=1.3  # ProteomeLM-Ess head training + calibration (07d)
# ProteomeLM backbone (07d) is installed separately (not on PyPI):
#   pip install "git+https://github.com/Bitbol-Lab/ProteomeLM.git"   # Apache-2.0; pins transformers~=4.51
